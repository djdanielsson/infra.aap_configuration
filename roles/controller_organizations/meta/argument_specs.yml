---
argument_specs:
  main:
    short_description: An Ansible Role to create organizations on Ansible gateway.
    options:
      aap_organizations:
        description: Data structure describing your organizations
        type: list
        required: true
        elements: dict
        options:
          custom_virtualenv:
            type: str
            description: ['Local absolute file path containing a custom Python virtualenv to use.', 'Only compatible with older versions of AWX/Tower', 'Deprecated, will be removed in the future']
          default_environment:
            type: str
            description: ['Default Execution Environment name, ID, or named URL to use for jobs owned by the Organization.']
          description:
            type: str
            description: ['The description to use for the organization.']
          galaxy_credentials:
            type: list
            description: ['list of Ansible Galaxy credential names, IDs, or named URLs to associate to the organization']
          instance_groups:
            type: list
            description: ['list of Instance Group names, IDs, or named URLs for this Organization to run on.']
          max_hosts:
            type: int
            description: ['The max hosts allowed in this organizations']
          name:
            required: True
            type: str
            description: ['Name to use for the organization.']
          new_name:
            type: str
            description: ['Setting this option will change the existing name (looked up via the name field.']
          notification_templates_approvals:
            type: list
            description: ['list of notifications to send on start']
          notification_templates_error:
            type: list
            description: ['list of notifications to send on error']
          notification_templates_started:
            type: list
            description: ['list of notifications to send on start']
          notification_templates_success:
            type: list
            description: ['list of notifications to send on success']
          state:
            default: present
            type: str
            description: ['Desired state of the resource.']

      # Async variables
      organizations_async_retries:
        default: "{{ aap_configuration_async_retries | default(30) }}"
        required: false
        description: This variable sets the number of retries to attempt for the role.
      aap_configuration_async_retries:
        default: 30
        required: false
        description: This variable sets number of retries across all roles as a default.
      organizations_async_delay:
        default: "{{ aap_configuration_async_delay | default(1) }}"
        required: false
        description: This variable sets delay between retries for the role.
      aap_configuration_async_delay:
        default: 1
        required: false
        description: This variable sets delay between retries across all roles as a default.
      aap_configuration_async_dir:
        default:
        required: false
        description: Sets the directory to write the results file for async tasks. The default value is set to `null` which uses the Ansible Default of `/root/.ansible_async/`.

      # No_log variables
      gateway_organizations_secure_logging:
        default: "{{ aap_configuration_secure_logging | default(false) }}"
        required: false
        type: bool
        description: Whether or not to include the sensitive tasks from this role in the log. Set this value to `true` if you will be providing your sensitive values from elsewhere.
      aap_configuration_secure_logging:
        default: false
        required: false
        type: bool
        description: This variable enables secure logging across all roles as a default.

      # Generic across all roles
      platform_state:
        default: present
        required: false
        description: The state all objects will take unless overridden by object default
        type: str
      aap_hostname:
        default: None
        required: false
        description: URL to the Ansible gateway Server.
        type: str
      aap_validate_certs:
        default: true
        required: false
        description: Whether or not to validate the Ansible gateway Server's SSL certificate.
        type: str
      aap_username:
        default: None
        required: false
        description: Admin User on the Ansible gateway Server. Either username / password or oauthtoken need to be specified.
        type: str
      aap_password:
        default: None
        required: false
        description: Gateway Admin User's password on the Ansible gateway Server. This should be stored in an Ansible Vault at vars/gateway-secrets.yml or elsewhere and called from a parent playbook. Either username / password or oauthtoken need to be specified.
        type: str
      aap_token:
        default: None
        required: false
        description: Gateway Admin User's token on the Ansible gateway Server. This should be stored in an Ansible Vault at or elsewhere and called from a parent playbook. Either username / password or oauthtoken need to be specified.
        type: str
...
